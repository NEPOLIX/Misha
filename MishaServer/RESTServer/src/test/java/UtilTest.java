import org.junit.Test;

/**
 * @author Behrooz Shahriari
 * @since 9/19/16
 */
public
class UtilTest
{
	 
	 @Test
	 public
	 void testBase64_ ( )
	 {
			
//			String text = "my Name is Hannah.";
//			for ( int len = 1 ; len < 256 ; ++len )
//			{
//				 byte array[] = new byte[ len ];
//				 for ( int j = 0, i = Byte.MIN_VALUE ; i <= Byte.MAX_VALUE && j < array.length ; j++, ++i ) array[ j ] = ( byte ) i;
//				 String encode = Base6X.toBase64_ ( array );
//				 System.out.println ( "ENCODE:" + encode + "\n" + encode.length ( ) );
//				 byte decode[] = Base6X.fromBase64_ ( encode );
//				 Assert.assertArrayEquals ( array, decode );
//			}
	 }
}
